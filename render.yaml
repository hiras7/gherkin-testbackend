services:
  - type: web
    name: gherkin-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn gherkin_backend:app --bind 0.0.0.0:$PORT
    envVars:
      - key: FRONTEND_ORIGIN
        value: "*"
